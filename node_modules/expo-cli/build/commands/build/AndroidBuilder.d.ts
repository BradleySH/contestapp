import { Credentials } from '@expo/xdl';
import BaseBuilder from './BaseBuilder';
import { Platform } from './constants';
export default class AndroidBuilder extends BaseBuilder {
    run(): Promise<void>;
    validateProject(): Promise<void>;
    _clearCredentials(): Promise<void>;
    collectAndValidateCredentials(): Promise<void>;
    checkEnv(): boolean;
    collectAndValidateCredentialsFromCI(credentialMetadata: Credentials.CredentialMetadata): Promise<void>;
    platform(): Platform;
}
